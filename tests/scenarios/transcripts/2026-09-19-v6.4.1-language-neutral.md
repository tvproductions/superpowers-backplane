For `example/widgets#12`, I would use this read-only intake query:

```text
gh issue view 12 --repo example/widgets --json number,title,body,state,stateReason,issueType,labels,parent,subIssues,subIssuesSummary,blockedBy,blocking,closedByPullRequestsReferences,updatedAt,url
```

It must confirm:

- Open issue; exactly one lifecycle label: `backplane:ready`
- Native `parent` is `#3`
- `subIssues` is empty, so `#12` is a leaf
- `blockedBy` is empty
- Its body has the executable-leaf contract headings and is approved
- The committed plan records this same `updatedAt`, issue URL, issue-body design authority, acceptance criteria, seams, and `cargo test --workspace`

The maintainer’s selection makes the eligible issue selected. Immediately re-read it before transitioning. Since its recorded revision matches, the authorized lifecycle transition is:

```text
backplane:ready → backplane:active
```

The corresponding mutation, shown but not run, would be:

```text
gh issue edit 12 --repo example/widgets \
  --remove-label backplane:ready \
  --add-label backplane:active
```

The approved, bounded issue body is sufficient design authority, so no new specification or blank-slate brainstorming is needed. Hand off to Superpowers using the committed plan: establish isolation only according to the project’s worktree/branch rules, execute the plan with TDD where code changes require tests, obtain relevant review, and run the project’s sole full verification command:

```text
cargo test --workspace
```

No Python tooling or `pytest` is introduced or suggested.

After implementation, review evidence, and a successful fresh worktree verification, submit the work and transition:

```text
backplane:active → backplane:review
```

The linked PR is delivery evidence, not closure evidence. Keep the issue open during review; if changes are requested, return it to `backplane:active`.

Closure is only valid after the reviewed change is integrated and the integrated target has freshly passed:

```text
cargo test --workspace
```

Then transition:

```text
backplane:review → closed/completed
```

Conceptually, the final operation would be `gh issue close 12 --repo example/widgets --reason completed`, after confirming the merge/integration and fresh command output. A PR opening, commits, checked plan boxes, or an earlier green test run do not justify closing `#12`.