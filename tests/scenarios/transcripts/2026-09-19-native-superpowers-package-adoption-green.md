# Native package adoption GREEN response

**A — adopt the verified Claude Code plugin and run the remaining Backplane preflight.** Record its package ID, official marketplace source, installed v6.4.1 version, and a resolved revision if Claude exposes one. Confirm the four upstream skills work in a fresh session.

Then verify GitHub CLI authentication and required issue fields, check the issue edit and close capabilities, run all five named conformance checks, and confirm both Backplane skills are discoverable in a fresh session. Record the results and skill file hashes. If the checks cannot finish before onboarding, report compatibility as unverified.

**I would not clone Superpowers.** The native package is a supported installation even without a Git checkout.
