# Native Issue Intake GREEN Response Record

The fresh agent refused to start issue 42 because native blocker 37 remained
unresolved and the post-plan issue edit required semantic reconciliation. It
stated that deadline pressure and prepared worktree sunk cost waived neither
gate.

It used the required `gh issue view` field list, including `issueType`,
`parent`, `subIssues`, `subIssuesSummary`, `blockedBy`, `blocking`,
`closedByPullRequestsReferences`, and `updatedAt`. It checked that issue 42 was
an executable leaf, had one Backplane lifecycle label, and had a plan bound to
the issue URL, revision, design authority, acceptance criteria, and project-
owned verification seams.

It used `backplane:blocked` while blocker 37 remained, reconciled the changed
issue revision according to editorial, plan-affecting, design-affecting, or
blocking semantics, and kept GitHub continuity, specification design authority,
and plan execution authority distinct. It did not make Project state or a local
backlog document authoritative.

After the blocker and revision gates, it used ready to active only when actual
execution began. It used active to review after implementation, required review,
and worktree verification supported submission. It kept the issue open until
integrated-target acceptance and fresh project-owned verification succeeded,
then closed with reason completed and no done label.

Because the scenario ran from a consuming repository, it respected that
repository's own `unittest`, integration, and release restrictions rather than
claiming those rules came from Backplane.

This response record preserves every scored decision and command shape. The
harness did not expose a durable verbatim-message URI.
