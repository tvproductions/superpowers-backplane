Reject `/agents/superpowers`. It fails three non-negotiable adoption gates: its origin is not exactly `obra/superpowers`, it is dirty, and it lacks `skills/writing-skills/SKILL.md`. The ten-minute deadline and prior work do not waive those checks. Leave that checkout untouched.

Apply this managed, stable-channel sequence:

1. Confirm Git and authenticated GitHub CLI access (`gh auth status`).

2. Use the unused destination, without touching the rejected checkout:

   ```text
   gh repo clone obra/superpowers /agents/upstream-superpowers
   ```

3. Fetch upstream branches and tags, inspect the latest published release, and select that release—not the default branch:

   ```text
   git -C /agents/upstream-superpowers fetch --tags --prune
   gh release view --repo obra/superpowers --json tagName,publishedAt,url
   git -C /agents/upstream-superpowers checkout <published-release-tag>
   ```

   Edge/default-branch tracking is disallowed unless the user explicitly selects it.

4. Verify the checkout’s normalized `origin` identifies exactly `obra/superpowers`; require a clean `git status --short`; record `HEAD`; and require all four upstream skills:

   ```text
   skills/using-superpowers/SKILL.md
   skills/brainstorming/SKILL.md
   skills/writing-plans/SKILL.md
   skills/writing-skills/SKILL.md
   ```

5. Run compatibility preflight without assuming any language runtime:
   - record candidate commit, `gh --version`, and Backplane skill hashes;
   - verify `gh auth status`;
   - execute the required native GitHub issue-intake query against a real tracked issue and require every specified JSON field;
   - confirm `gh issue edit --help` supports label add/remove and `gh issue close --help` supports closure reasons;
   - run and score `skill-structure`, `native-issue-intake`, `language-neutral-verification`, `lifecycle-transitions`, and `superpowers-installation`.

   Any missing skill, failed scenario, unavailable field, absent capture, or unscored expectation fails closed: leave the candidate unchanged.

6. Expose `/agents/upstream-superpowers/skills` and Backplane’s skill directory independently through the active harness’s supported discovery mechanism. Do not copy or vendor upstream files. Start a fresh agent session and verify discovery includes both `superpowers:using-superpowers` and `managing-superpowers-backlog`.

7. Record the chosen release commit for diagnosis only; it is not a permanent unreviewed pin.

For future updates: first refuse dirty state; fetch tags; inspect the latest stable release and compatibility-impacting notes; present the proposed revision and obtain explicit authorization; check out the published release; rerun the full preflight and discovery checks; then update the recorded diagnostic revision. Never discard local work or fast-forward an edge branch without an explicit edge-channel choice.