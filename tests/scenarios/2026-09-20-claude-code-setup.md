# Claude Code setup and upstream adoption evidence for #18

## Task 1 — current baseline (RED)

Observed 2026-09-20 on `issue/18-claude-setup` at base `4c94aa627e4b59da5e7c942e9d42c04edac56158`. This baseline does not score a live #18 setup run. The integrated #17 package and guide match published and tested commit `d42cff1ce4fb03a66ca0daf54d27fd78e2cbfb6a` by `git diff --exit-code` over `.claude-plugin`, `README.md`, the Claude guide, and both canonical skills. `gh api` resolved that exact published SHA. The pinned detached clone is clean at `C:\Users\Jeff\AppData\Local\Temp\backplane-claude-17-source-c9121b72c27945cb92e74fbaec21284d\superpowers-backplane`, with exact `https://github.com/tvproductions/superpowers-backplane.git` origin.

The current guide has the #17 published-commit clone, one Backplane installation, complete read-only issue intake, setup request, and three-skill discovery. It lacks distinct native upstream, sibling checkout, and absent-upstream operator procedures and the full #18 failure/recovery table. Neither this scenario nor its transcript existed before Task 1. Those are the RED gaps for the guide and host evidence; #17's smoke is historical context, not a #18 result.

### Current host and sources

| Check | Observed baseline |
|---|---|
| Claude Code | `2.1.241 (Claude Code)`; `claude plugin marketplace list --help` and `claude plugin list --help` expose `--json`; `claude plugin install --help` exposes `plugin@marketplace` and `--scope`. |
| Default Claude profile | `claude auth status --json`: `loggedIn: false`, `authMethod: none` (command exit 1). It is not used for live checks. |
| Disposable Claude profile | `C:\Users\Jeff\AppData\Local\Temp\backplane-claude-17-241ec92c05e84296bc7a2df74be839d3`; `loggedIn: true`, `authMethod: claude.ai`. Only `CLAUDE_CONFIG_DIR` in child process scope was changed. No auth values were copied or recorded. |
| Disposable Backplane | One enabled `superpowers-backplane@superpowers-backplane` v0.1.0, user scope; marketplace `superpowers-backplane` is the clean pinned clone above. |
| Disposable upstream | One enabled `superpowers@superpowers-dev` v6.4.1, user scope; marketplace is the separately installed `.agents/superpowers` checkout. This is an authoritative development marketplace, not the official absent-upstream channel. |
| Project Git | Root `C:/Users/Jeff/source/repos/agents/superpowers-backplane`; origin `https://github.com/tvproductions/superpowers-backplane.git`; clean issue branch at base commit above. |
| Upstream Git | Exact `https://github.com/obra/superpowers.git` origin, clean `v6.4.1` checkout at `5bf4e78011075bcfc0dc295f0724994cd123ee71`. Manifest version `6.4.1`; `using-superpowers`, `brainstorming`, `writing-plans`, and `writing-skills` files present. |
| GitHub CLI | `gh auth status` succeeded as `ahuimanu`; complete 15-field read-only #18 intake succeeded. `gh issue edit --help` exposes `--add-label` and `--remove-label`; `gh issue close --help` exposes `--reason completed`. |
| Issue state | #18 OPEN, `backplane:active`, parent #1, #17 CLOSED blocker, #19 OPEN successor; observed revision `2026-09-20T17:29:58Z`. No setup probe has changed it. |

Package SHA-256: `.claude-plugin/plugin.json` `32BBD2859083522CF3FC040B45864A1DE6FC1FCD370C5BF30C21F87086486565`; `.claude-plugin/marketplace.json` `E9903B166D7FE302A51280A75D613D019A8945D8BEE23A77819BB2E2AE839DA1`; current guide `4409A700BFF6DB77C7CB852B747C36EEF7F095BB015BC6513EFE7385D35EC8B5`.

Authoritative documentation rechecked: [Claude plugin structure](https://code.claude.com/docs/en/plugins), [marketplace CLI and JSON inventory](https://code.claude.com/docs/en/plugin-marketplaces), [native installation](https://code.claude.com/docs/en/discover-plugins), [configuration environment](https://code.claude.com/docs/en/env-vars), and the installed upstream README Claude Code section. Upstream lists `/plugin install superpowers@claude-plugins-official` as its official marketplace path and `obra/superpowers-marketplace` as an alternative. `--plugin-dir` is a session-scoped local plugin input. `CLAUDE_CONFIG_DIR` isolates Claude configuration.

### Supported setup modes

| Mode | Input/source | Expected result | Actual result | Preservation evidence | Score |
|---|---|---|---|---|---|
| Native upstream package | Authenticated #17 disposable profile with separate `superpowers@superpowers-dev` and pinned Backplane plugin | Authoritative source/version, `gh` preflight, and three direct fresh skill invocations | `superpowers@superpowers-dev` v6.4.1 from authoritative clean `obra/superpowers` checkout; full `gh` capability; three separate actual Skill tool calls from distinct package roots | Three plugin configuration SHA-256 hashes unchanged; upstream HEAD/status and full #18 fields unchanged | PASS |
| Sibling checkout | Backplane-only second profile; authoritative `.agents/superpowers` via session `--plugin-dir` | No upstream relocation or update; fresh three-skill discovery | Clean `obra/superpowers` HEAD `5bf4e78011075bcfc0dc295f0724994cd123ee71` and required skills; full `gh` capability; three separate actual Skill tool calls | Three second-profile plugin configuration hashes unchanged; only Backplane remains installed; upstream HEAD/status and full #18 fields unchanged | PASS |
| Absent upstream | Second authenticated disposable Backplane-only profile; official Anthropic marketplace absent | Setup installs upstream through the upstream-documented official channel in the same session; fresh discovery | Same setup session registered anthropics/claude-plugins-official and installed superpowers@claude-plugins-official v6.3.0; three fresh direct Skill calls passed | Backplane identity retained; sentinel and complete #18 issue JSON unchanged; expected profile plugin config changes recorded | PASS |

### Failure and recovery probes

| Case | Input/source | Expected result | Actual result | Preservation evidence | Score |
|---|---|---|---|---|---|
| Lookalike upstream source | Clean pinned clone with obrafake/superpowers origin and observable v6.4.1 | Reject false origin before adoption | Origin was read and rejected; recovery names authoritative unused source | Plugin, fixture, sentinel, and complete #18 fields unchanged | PASS |
| Versionless native upstream package | Installed local upstream fixture with official repository URL field, no version field, and no Git revision | Stay UNKNOWN; require authoritative source and observable version/revision | Claude plugin inventory reported version unknown; live session refused compatibility; self-declared URL was not accepted as provenance | Plugin, both manifests, fixture, sentinel, and complete #18 fields unchanged | PASS |
| Duplicate effective Backplane | Second disposable marketplace identity backplane-conflict | Stop and identify both IDs before setup | Both enabled plugin IDs and scopes were identified; setup stopped | Both plugin IDs, marketplaces, config hashes, fixture, and complete #18 fields unchanged before test-only cleanup | PASS |
| Dirty authoritative checkout adoption | obra/superpowers fixture with modified README.md | Adopt in place without update | Provenance/revision/skills and three direct Skill calls passed; checkout remained dirty | Profile config, fixture README hash/HEAD/status, and complete #18 fields unchanged | PASS |
| Dirty checkout update refusal | Same dirty authoritative fixture, explicit update request | Refuse update before mutation | Refused on modified README.md; no fetch, checkout, or plugin write | Profile config, fixture README hash/HEAD/status, and complete #18 fields unchanged | PASS |
| Occupied checkout path | Non-Git destination containing sentinel.txt | Stop; choose unused path without deletion | Refused clone into occupied path and gave non-destructive recovery | Sentinel hash, profile config, and complete #18 fields unchanged | PASS |
| Missing gh auth | Temporary Git Bash gh wrapper returning auth failure | Stop with auth repair | Claude saw gh fixture capability unavailable, stopped setup, and named repair | Profile config, upstream fixture, sentinel, and complete #18 fields unchanged | PASS |
| Missing gh intake field | Same wrapper rejecting required closedByPullRequestsReferences query | Stop with compatible gh repair | Claude saw the field error, stopped intake, and named repair | Profile config, upstream fixture, sentinel, and complete #18 fields unchanged | PASS |
| Missing gh label capability | Same wrapper omitting label add/remove flags | Stop with compatible gh repair | Claude saw both flags absent and stopped setup | Profile config, upstream fixture, sentinel, and complete #18 fields unchanged | PASS |
| Missing gh closure reason | Same wrapper omitting closure reason flag | Stop with compatible gh repair | Claude saw reason flag absent and stopped setup | Profile config, upstream fixture, sentinel, and complete #18 fields unchanged | PASS |
| Missing required upstream skill | Authoritative clone missing tracked writing-plans/SKILL.md | Stop without repairing checkout | Claude observed missing skill, gave owner-directed repair, and stopped | Profile config, fixture HEAD/status/file hashes, sentinel, and complete #18 fields unchanged | PASS |
| Unauthenticated disposable profile | Fresh empty temporary Claude profile, no login attempted | Keep setup UNKNOWN and request one authentication | Claude auth status returned exit 1, loggedIn false, authMethod none; no setup session launched | No plugins installed and complete #18 fields unchanged | PASS |

`UNKNOWN` here means not yet observed for #18. Each later PASS requires actual host output plus before/after preservation evidence in the linked transcript; a model's final assertion alone does not establish discovery or preservation.

## Task 2 — setup guide RED to GREEN

RED search on the baseline guide found no `claude plugin marketplace list --json`, `claude plugin list --json`, `--plugin-dir`, or `claude-plugins-official` operator step. The only `UNKNOWN` rule was a general response instruction. It had no mode-specific source/version records, duplicate Backplane recovery, dirty upstream update refusal, missing `gh` capability recovery, or complete failure table. The existing occupied Backplane clone-path guard and clean reviewed-Backplane checkout check were present; those are not upstream adoption probes.

GREEN guide keeps the #17 published 40-character Backplane clone and native installation block intact. It now orders plugin inventory, upstream classification, authoritative provenance/version plus four required skills, `gh` capabilities, and three direct fresh Skill tool checks. The native mode adopts an installed package in place; sibling mode records exact `obra/superpowers` origin, full HEAD, status, required skills, and passes `--plugin-dir` to setup and each fresh session; absent mode delegates official stable-channel installation to the interactive setup session with host approval and records the resulting independent plugin identity. The failure table states stop, repair, and preservation for unknown/versionless source, duplicates, dirty update, occupied destination, `gh` failures, incompatible upstream, and unsigned-in Claude. Issue intake remains read-only, and #19 still owns lifecycle verification. Tasks 3–5 must replay these rules before treating any row as PASS.

Task 2 document gate: all four PowerShell code blocks parsed with zero errors; 12 guide contract anchors were present; `git diff --check` passed; `claude plugin validate --strict .` returned Validation passed. The package manifest and marketplace bytes were not edited.

## Task 3 — native and sibling live adoption

The native profile remained authenticated. The second disposable profile `C:\Users\Jeff\AppData\Local\Temp\backplane-claude-18-62f1d7dda2704635a266bdacedfd7c3f` was empty at creation, authenticated once through host browser login, then given only the published pinned Backplane plugin from `d42cff1ce4fb03a66ca0daf54d27fd78e2cbfb6a`. Initial and final second-profile plugin inventory contain that one enabled `superpowers-backplane@superpowers-backplane` v0.1.0 and no installed upstream plugin. Normal Claude profile was not used or modified by these setup commands.

Both setup calls ran the approved host recipe in neutral temporary project directories. Both reached `error_max_turns` at turn 21 without a final answer. Each original session was resumed with tools disabled solely to report checks already performed; the mode score also uses independent CLI and session records, never the model report alone. The native setup session was `7a45122c-9463-4db8-acb7-fed45053714b`; the sibling setup session was `b2b0b833-60cf-4df2-8ed8-83e54763d870`. [Exact prompts, relevant unedited responses, and six actual Skill tool-use records](transcripts/2026-09-20-claude-code-setup.md) are retained without credential contents.

Native and sibling modes each made one setup request and three *separate fresh* direct Skill invocations. Each session JSONL has exactly one `Skill` tool-use input for its named skill, with the upstream `using-superpowers` base at `.agents/superpowers/skills/using-superpowers` and the two Backplane bases under the pinned temporary Backplane checkout. The sibling's third model response drifted into a plan-mode explanation after loading `managing-superpowers-handoffs`; its actual Skill call and returned base directory still prove discovery. The `/help` custom-command listing was not observed in these noninteractive sessions; direct Skill loading is the scored behavior.

Native profile plugin configuration SHA-256 values before and after: `installed_plugins.json` `FFC4C753CA81181F6A4F70078945349118C82B19481BC7A648DBC8373DCFDE73`, `known_marketplaces.json` `B61C28CD67301D2F020EC92B9684372A99F09D0EE1258019FE9A8ACF133804B7`, `settings.json` `6EA3208091603C230632A2C6569F2A88DAD8D658C041129BD331D5CF89A855DA`. Second profile: `installed_plugins.json` `2FA8C7956AAB2D60976C59FDE06FA8FD3C527471618E3888BC5D5AA2FBBCF7D6`, `known_marketplaces.json` `699762F3DBCFE67DFD12676D3B0B96199D5DDE3C130C78E3AB50E10122929CCC`, `settings.json` `1EC4D98419F857925B628B98461B1DF6C6036F9C6267D73843C5F526B5696C43`. These comparisons exclude session metadata and credential files. Upstream HEAD remained `5bf4e78011075bcfc0dc295f0724994cd123ee71`, with empty porcelain status. Full native #18 intake before and after each mode remained OPEN, `backplane:active`, revision `2026-09-20T17:29:58Z`, parent #1, closed blocker #17, and open successor #19. Neither mode installed, cloned, updated, or relocated upstream.

## Task 4 — isolated failure stops and preservation

Observed on 2026-09-20 in the authenticated second disposable Claude profile from Task 3. The profile started this task with only the pinned Backplane plugin. The fixture parent was C:\Users\Jeff\AppData\Local\Temp\backplane-claude-18-5653d382bf8440e98caf9b365e6f5faa. Its clean, dirty, missing-skill, and lookalike checkouts came from obra/superpowers at 5bf4e78011075bcfc0dc295f0724994cd123ee71. Only the dirty fixture had a modified README.md; only the missing-skill fixture had a deleted writing-plans/SKILL.md; only the lookalike fixture had its origin changed to obrafake/superpowers. The occupied fixture contained sentinel.txt.

Each scored Claude probe used a fresh neutral temporary project, the authenticated disposable profile, normal permission mode, an explicit read-only tool allowlist, and a before/after snapshot taken before fixture teardown. The snapshots compared three plugin configuration hashes, installed plugin IDs, marketplace names, the occupied sentinel hash, fixture source/HEAD/status/file hashes, and the complete native #18 issue JSON. All eleven scored before/after comparisons passed. Every #18 snapshot retained OPEN/backplane:active, parent #1, closed blocker #17, open successor #19, and updatedAt 2026-09-20T17:29:58Z. The full prompts and unedited host responses are in the companion transcript.

| Case | Claude session | Observed stop or adoption |
|---|---|---|
| Lookalike source | 391a088e-0588-4503-b87f-6b1b9839b6e6 | Read obrafake/superpowers origin and rejected adoption. |
| Missing writing-plans skill | e7082d57-10e8-4329-b425-e7c7a51e59a7 | Observed the missing tracked skill, stopped, and gave an owner repair. |
| gh auth | a8eaf771-508b-4144-b868-a6cfd0a0547c | Observed the staged auth failure and stopped. |
| gh intake field | 399e9532-644b-4f0d-9d79-0a60e56a1f64 | Observed the required JSON field rejection and stopped. |
| gh label flags | 4b40e38d-ac0e-47a6-bb5d-8f9838281cf1 | Observed missing add/remove label flags and stopped. |
| gh closure reason | 357c3e56-c206-4a97-ba68-fa4bb1928c74 | Observed missing reason flag and stopped. |
| Dirty authoritative adoption | 8b07e3d3-ca47-4467-b43f-63c4887d8ac3 | Adopted the dirty checkout in place; the JSONL records separate Skill calls for using-superpowers, managing-superpowers-backlog, and managing-superpowers-handoffs. |
| Dirty update refusal | 6ad28892-cdae-4caa-98f7-2131fa150e4f | Refused the requested update on modified README.md before fetch or checkout. |
| Occupied destination | ac31f870-8a9b-4d8e-a540-65a5f3ce62de | Refused to clone into the directory containing sentinel.txt. |
| Duplicate Backplane | 51bc2750-544b-4d95-b0cf-4f244a3ad5f1 | Detected both enabled user-scope Backplane IDs and stopped. |
| Versionless native plugin | 1a4f8fba-fd2f-4e6b-815d-c23915039fec | Initial run reached its turn limit; a tool-free continuation from the same session reported UNKNOWN and refused compatibility. |

The gh fixture used both gh.cmd for PowerShell and an executable gh wrapper for Claude Code Git Bash. A local four-by-four capability check proved each selected failure only: auth and intake exited nonzero in their own case; label-help omitted add/remove flags only in its case; closure-help omitted reason only in its case. The wrapper forwards only read-only preflight operations and rejects mutation commands. The actual Claude sessions observed those four distinct failures. The first auth attempt is excluded: Git Bash bypassed gh.cmd, reached the real gh, and hit the turn limit. The subsequent wrapper-backed auth run is the scored one.

The versionless fixture was an archive of the verified clean upstream checkout, with the four required SKILL.md hashes retained. It omitted version from plugin.json and marketplace.json and had no Git repository. Its manifest retained the declared repository URL https://github.com/obra/superpowers, which is a self-asserted field, not provenance. Claude plugin validate passed with a missing-version warning; strict validation refused the warning. Native plugin installation succeeded in the disposable profile, and claude plugin list --json reported superpowers@versionless-superpowers with version unknown. The setup session observed no installed version or revision, treated the URL field as unverified, and refused compatibility. Its read-only synthetic precursor also returned UNKNOWN, but the scored row uses the live package and fresh session. This fixture establishes versionless fail-closed behavior; it does not claim an authoritative upstream package.

The duplicate and versionless packages were each added only for their isolated test. Their before/after comparisons were made while both IDs were still installed. Each test-only plugin and marketplace was then removed by its exact ID. The original pinned Backplane plugin and marketplace remained the only enabled entries. Their installed_plugins.json and known_marketplaces.json hashes returned to the Task 3 values. The profile settings.json hash had already changed from the Task 3 value during the first interactive TUI attempt, which entered first-run login-method onboarding despite claude auth status reporting signed in. That TUI attempt was exited without another login and is not a scored preservation probe. From the first print-mode baseline onward, every scored snapshot preserved the then-current settings.json hash B5778B7CAF6194FEFA235FB252113E876D8A77626D354A1759A9253BF6615F30. Its final enabledPlugins and extraKnownMarketplaces each contain only the original Backplane identity.

A separate fresh empty temporary Claude profile returned claude auth status exit 1, loggedIn false, authMethod none, and zero plugins. No login or setup session was started. This is an expected unmet prerequisite, not a supported-mode PASS; complete #18 issue fields before and after were identical. The signed-in second profile remained available for Task 5.

No normal Claude profile, this repository's .agents/superpowers checkout, canonical skills, Claude manifests, or GitHub issue state was changed by Task 4.

## Task 5 — official absent-upstream installation

Observed 2026-09-20 in the same authenticated second disposable Claude profile, without a sibling plugin directory or gh shim. The initial native inventory showed exactly one enabled Backplane plugin, version 0.1.0, and its own marketplace; no upstream plugin or official marketplace was registered. The source was the upstream Superpowers README Claude Code installation section and Anthropic's plugin marketplace documentation. Independent host inspection of anthropics/claude-plugins-official at catalog commit c447c3207a425bc4e2a0d068435f64b0477ae981 found its superpowers entry, sourced from obra/superpowers at b36e0829c6d0140e93cfef2ca599b1b07d4a7797. That source manifest reports version 6.3.0. The upstream release repository currently reports v6.4.1; the official catalog lags that release, and the selected channel's available package is v6.3.0. Its pinned revision is Backplane's documented known-good baseline.

The interactive TUI entered first-run onboarding during Task 4 despite an authenticated profile, so this probe used print mode and resumed the same Claude session ID d904387e-bd0d-456c-b8e0-3007c66a3b0d after reviewing its exact proposal. Initial install failed because the official marketplace was not registered in the isolated profile. The session incorrectly inferred from that failure that the official catalog lacked Superpowers. Independent catalog inspection corrected the inference. The same session then executed claude plugin marketplace add anthropics/claude-plugins-official and claude plugin install superpowers@claude-plugins-official --scope user; both tool results reported success. A combined marketplace-add plus echo command was denied by the permission layer and made no change before the exact add succeeded. The final native inventory shows separate enabled Backplane 0.1.0 and Superpowers 6.3.0 identities, each user scope and each from its own marketplace. The cached official plugin manifest and four required SKILL.md files matched the catalog-pinned upstream Git commit byte for byte.

A new Claude setup session 1a1b112d-0dd6-41f4-bd26-549ac605707d reported official source, version, required skill files, one Backplane identity, complete gh auth/intake/label/closure capability, and unchanged #18 fields. Its first print call reached the turn limit; a tool-free continuation produced the recorded verdict. Three separate fresh sessions made one actual Skill tool call each: 939988c3-5634-4757-ac6a-102bc03caec5 loaded superpowers:using-superpowers, fa0f7867-b80c-46c7-b712-78958d0c7a31 loaded superpowers-backplane:managing-superpowers-backlog, and 8e87377f-2646-4bc6-920d-f882d52d0b76 loaded superpowers-backplane:managing-superpowers-handoffs. Their raw JSONL calls and returned skill roots were checked. The five integrated conformance checks and full label inventory were not part of this supported-mode probe and remain for Task 6.

The before and after native #18 JSON SHA-256 remained DA6BB296FCEF34C1F2DD1A46A6CC9AA88D5F8C8596C262B4EC5457B5A1513825, with OPEN/backplane:active, parent #1, closed blocker #17, open successor #19, and revision 2026-09-20T17:29:58Z. The occupied fixture sentinel SHA-256 remained 108AF2B4D4067706073F3D0E85459A6F907A4E35D6E74235BE7BC5D4F60CC12E. Plugin config hashes changed after the official installation: installed_plugins.json 2FA8C795 to 04F9CC2A, known_marketplaces.json 699762F3 to 513DA722, and settings.json B5778B7C to 7EFEE62A. These are hash prefixes; complete before/after hashes are in the local snapshots. The original Backplane ID, version, scope, enabled state, and marketplace path remained the same. Neither the normal Claude profile, repository upstream checkout, canonical skills, manifests, nor GitHub issue state changed.
