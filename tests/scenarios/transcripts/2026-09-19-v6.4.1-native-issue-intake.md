I read `managing-superpowers-backlog`, all three of its references, the required `superpowers:using-superpowers` skill, and its Codex reference.

## Decision now

**Do not start implementation.**

Issue #42 is:

- **Captured:** yes.
- **Covered:** yes; it has an approved specification and plan.
- **Selected:** yes; the maintainer selected it.
- **Eligible:** no.
- **Complete:** no.

The native blocker #37 makes it ineligible. The later body edit also requires reconciliation before the plan can be treated as current. The release deadline, maintainer authority, and prepared worktree do not waive either gate.

The evidence supported state is **`backplane:blocked`**.

## Read-only intake

I would first obtain the repository identity and complete native issue state:

```powershell
gh auth status
$repo = gh repo view --json nameWithOwner --jq '.nameWithOwner'

$fields = 'number,title,body,state,stateReason,issueType,labels,parent,subIssues,subIssuesSummary,blockedBy,blocking,closedByPullRequestsReferences,updatedAt,url'

$issue42 = gh issue view 42 --repo $repo --json $fields | ConvertFrom-Json
gh issue view 42 --repo $repo --comments

$issue37 = gh issue view 37 --repo $repo --json $fields | ConvertFrom-Json
gh issue view 37 --repo $repo --comments

gh label list --repo $repo --limit 100 --json name,description
```

I would verify:

1. #42 is open.
2. `subIssues` is empty, making it an executable leaf. Being a child of an epic is valid and does not itself affect eligibility.
3. Its body contains every required heading.
4. Its sole lifecycle label is exactly one `backplane:*` label.
5. The linked specification and plan are approved and committed.
6. The plan records #42’s URL, consumed `updatedAt`, design authority, acceptance criteria, verification seams, and exact project owned verification commands.
7. #37 remains unresolved.
8. The current `updatedAt` differs from the revision recorded in the plan.

Zero or multiple Backplane labels is invalid state. I would fail closed and repair that state before attempting another transition.

## Immediate state change

If #42 currently carries `backplane:ready`, I would apply this single label transition:

```powershell
gh issue edit 42 --repo $repo `
  --remove-label 'backplane:ready' `
  --add-label 'backplane:blocked'
```

Then record the resume target and release conditions:

```powershell
$comment = @'
Resume target: backplane:ready

Issue #42 is blocked by native dependency #37. Resume only after #37 is resolved, the current issue body is reconciled against the approved specification and plan, the plan consumes the current issue semantics, and every ready-state gate is revalidated.
'@

gh issue comment 42 --repo $repo --body $comment
```

If it is already `backplane:blocked`, I would leave the label unchanged and ensure that the required resume comment exists.

I would then re-fetch #42 and verify that it has exactly `backplane:blocked` plus its unchanged unrelated labels:

```powershell
gh issue view 42 --repo $repo --json $fields `
  --jq '{state,updatedAt,labels:[.labels[].name],blockedBy,parent,subIssues}'
```

## Reconcile the body edit

`updatedAt` is a conservative change detector. It proves that reconciliation is required, but does not prove that the plan is invalid.

I would compare the current issue body with the linked approved specification and plan:

- **Editorial and already covered:** record that conclusion and update the plan’s consumed revision to the current `updatedAt`.
- **Acceptance or scope change covered by the specification but absent from the plan:** revise the plan with `superpowers:writing-plans`, obtain approval, and record the new revision.
- **Design change:** use `superpowers:brainstorming`, revise and approve the specification, then regenerate or revise the plan.
- **Additional blocker:** represent it as a native GitHub dependency and keep the issue blocked.

This reconciliation can happen while #37 remains open, reducing delay without beginning implementation.

## Authority

The authoritative layers are:

1. Repository architecture and control documents govern product constraints, verification, branch, and release rules.
2. GitHub issue #42 and its native relationships govern backlog identity, objective, scope, acceptance criteria, hierarchy, dependencies, and lifecycle.
3. The approved committed specification governs design.
4. The current approved Superpowers plan governs execution of #42.
5. Commits, reviews, the integrated pull request, and fresh verification provide completion evidence.

The plan is temporarily stale until the revision drift is reconciled. Plan checkboxes, the prepared worktree, and GitHub Project fields are not completion or eligibility evidence.

## Resuming and starting

After #37 is resolved, I would re-fetch both issues and revalidate every eligibility gate. If reconciliation is complete, transition blocked to ready:

```powershell
gh issue edit 42 --repo $repo `
  --remove-label 'backplane:blocked' `
  --add-label 'backplane:ready'
```

The maintainer’s selection then becomes actionable. Only when the first plan task actually begins would I transition ready to active:

```powershell
gh issue edit 42 --repo $repo `
  --remove-label 'backplane:ready' `
  --add-label 'backplane:active'
```

Execution then follows the approved plan through the applicable Superpowers execution, TDD, review, verification, and branch finishing skills. Before any Git mutation, I would verify that `git rev-parse --show-toplevel` resolves to the intended repository root.

## Submission and closure

After implementation, required review, and worktree verification support submission:

```powershell
gh issue edit 42 --repo $repo `
  --remove-label 'backplane:active' `
  --add-label 'backplane:review'
```

The issue remains open during PR review and integration. Requested implementation changes produce `review → active`.

Issue #42 closes only after:

1. The delivery is merged or otherwise integrated.
2. The integrated target satisfies every acceptance criterion.
3. The exact project owned verification commands run freshly against that integrated target.
4. The evidence is recorded.

Then:

```powershell
gh issue close 42 --repo $repo --reason completed `
  --comment 'Integrated delivery satisfies the acceptance criteria, and fresh project-owned verification passed on the integrated target.'
```

PR creation, approval, merge readiness, or checked plan tasks do not close the issue. Explicit abandonment would instead use `--reason 'not planned'`. Closing #42 also does not automatically prove that its parent epic is complete.

No GitHub issue or local file was mutated here. The governing references are [github-issue-contract.md](C:/Users/Jeff/source/repos/agents/superpowers-backplane/.agents/skills/managing-superpowers-backlog/references/github-issue-contract.md) and [superpowers-binding.md](C:/Users/Jeff/source/repos/agents/superpowers-backplane/.agents/skills/managing-superpowers-backlog/references/superpowers-binding.md).